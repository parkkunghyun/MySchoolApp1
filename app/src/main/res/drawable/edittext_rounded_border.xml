<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <corners android:radius="8dp" /> <!-- 테두리의 각을 조절하여 둥근 테두리를 만듭니다. -->
    <stroke
        android:width="2dp"
        android:color="#aeaeae" /> <!-- 테두리 선의 두께와 색상을 설정합니다. -->
</shape>